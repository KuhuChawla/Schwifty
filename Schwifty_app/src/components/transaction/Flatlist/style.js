import { Dimensions, StyleSheet } from "react-native";
const styles = StyleSheet.create({
    text: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 20
    },
    container: {
        flex: 1,
        paddingTop: 22,
        width: 100,
       },
       item: {
         padding: 10,
         fontSize: 18,
         height: 44,
       },
})

export default styles